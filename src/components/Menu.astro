---
const base = import.meta.env.BASE_URL;
---

<ul class="menu">
  <li><a href={`${base}#soluciones`} class="menu__link">Soluciones</a></li>
  <li><a href={`${base}blog/`} class="menu__link">Blog</a></li>
  <li><a href={`${base}contacto/`} class="menu__cta">Cotizar proyecto</a></li>
</ul>

<style>
  .menu {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 1.5rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .menu li {
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .menu a {
    position: relative;
    display: inline-flex;
    align-items: center;
    min-height: 40px;
    color: rgba(255, 255, 255, 0.78);
    font-family: var(--font-heading);
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    transition:
      color 0.25s ease,
      transform 0.25s ease,
      opacity 0.25s ease;
  }

  .menu a::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: 0.35rem;
    width: 100%;
    height: 1px;
    background: var(--color-accent);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.25s ease;
  }

  .menu a:hover {
    color: #ffffff;
  }

  .menu a:hover::after {
    transform: scaleX(1);
  }

  .menu__cta {
    min-height: 42px;
    padding: 0.8rem 1.15rem;
    background: var(--color-accent);
    color: #ffffff !important;
    justify-content: center;
    white-space: nowrap;
    border-radius: 0;
  }

  .menu__cta::after {
    display: none;
  }

  .menu__cta:hover {
    background: var(--color-accent-hover);
    color: #ffffff !important;
    transform: translateY(-1px);
  }

  @media (max-width: 900px) {
    .menu {
      gap: 1rem;
    }
  }

  @media (max-width: 768px) {
    .menu {
      flex-direction: column;
      align-items: stretch;
      gap: 0;
      width: 100%;
    }

    .menu li {
      width: 100%;
      border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    }

    .menu li:last-child {
      border-bottom: none;
      margin-top: 1.5rem;
    }

    .menu__link {
      width: 100%;
      padding: 1.2rem 0;
      font-size: 0.9rem;
      min-height: auto;
    }

    .menu__link::after {
      display: none;
    }

    .menu__cta {
      width: 100%;
      text-align: center;
      padding: 1rem 1.5rem;
      min-height: 48px;
    }
  }
</style>

<!-- <script>
  // Cerrar el menú móvil al hacer click en cualquier enlace
  document.addEventListener('DOMContentLoaded', () => {
    const menuLinks = document.querySelectorAll('.menu a');
    const toggle = document.querySelector('.navbar__toggle');
    const menu = document.querySelector('.navbar__menu');
    
    menuLinks.forEach(link => {
      link.addEventListener('click', () => {
        if (window.innerWidth <= 768 && toggle && menu) {
          toggle.setAttribute('aria-expanded', 'false');
          menu.classList.remove('is-open');
          document.body.style.overflow = '';
        }
      });
    });
  });
</script> -->